Currently, to run the app, make sure you have NPM and flask installed. (there may be other dependencies as well)

from frontend folder, run "npm run start"

from backend folder, activate the virtual env (source bin/venv/activate). 

then, http://localhost:3000/ should have the app running
